# EU Reference — bin/script2flash
# Category: flash_script
# Flash helper scripts (updater-script / flash-all style).
# DeadZone equivalent: factory/output/flash_scripts.py (image-driven, no hardcoded list).
# WARNING: packROM.sh uses `du -sb` for lpmake sizing — NEVER import that logic.
# DO NOT execute directly.
