# EU Reference — bin/ddevice
# Category: device_metadata
# These scripts parse ROM identity (Android version, region, device codename,
# chip, slot) and write metadata sidecar files.
# DeadZone equivalent: factory/pipeline/resolver.py + registry/ YAML.
# DO NOT execute directly.
