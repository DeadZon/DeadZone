id: deadzone_legend
name: DeadZone Legend
version: "1.0"
runner: factory.patch.legend.runner
description: >
  DeadZone Legend full mod package — self-contained under factory/patch/legend/.
  All patches, assets, actions, patchers, reports, and validators live here.

home: factory/patch/legend

entrypoint: factory.patch.legend.runner.run_legend

steps:
  - validate_home
  - fstab
  - props
  - apk_mods
  - jar_mods
  - systemui_assets
  - permissions

assets: factory/patch/legend/assets
patchers: factory/patch/legend/patchers
actions: factory/patch/legend/actions
reports: factory/patch/legend/reports
